node_modules
dist
.DS_Store
*.log
.vite
.env
.env.local
__pycache__
*.pyc
venv/
.venv/
*.mp4
*.mov
.wrangler/

# SME Profile: real client data (sensitive, do not commit).
# tuman_mfy.xlsx is public reference data and stays committed.
backend/data/sme_profile/ruyxat.xlsx
backend/data/sme_profile/Руйхат-2*.xlsx

# CERR Mahalla Analytics v2 — 1.4 GB scraped JSON tree.
# Source data lives outside git (Railway Volume in prod, local fs in dev).
# Path is configurable via CERR_DATA_ROOT env var; default points here.
reference_analytics_platform/cerr_runs/
# Keep the legacy mirrored Next.js bundle out of git too — it's only used
# as a reference; v2 is rebuilt natively in Vue.
reference_analytics_platform/platform/
