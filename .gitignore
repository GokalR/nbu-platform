node_modules
dist
.DS_Store
*.log
.vite
.env
.env.local
__pycache__
*.pyc
venv/
.venv/
*.mp4
*.mov
.wrangler/

# SME Profile: real client data (sensitive, do not commit).
# tuman_mfy.xlsx is public reference data and stays committed.
backend/data/sme_profile/ruyxat.xlsx
backend/data/sme_profile/Руйхат-2*.xlsx

# CERR Mahalla Analytics v2 — 1.4 GB scraped JSON tree.
# Source data lives outside git (R2 bucket nbu-cerr-data, served at
# https://cerr-data.devgokal.com). Path is configurable via CERR_DATA_ROOT.
reference_analytics_platform/cerr_runs/
# But DO commit the Next.js bundle + Flask server — it's the actual UI
# served as a separate Railway service and iframe'd from the main app.
reference_analytics_platform/platform/__pycache__/
